This folder is where the thumbnail images are saved.

By default that are two images already in this folder.

noimg.gif - This is the image that defaults for the user profiles if none exist
noimgloggedin.gif - This is the image that defaults when a user logs in and does not have a profile pic.